# Frontend config. Copy to `.env.local` and adjust for local dev.
# (Vite only exposes vars prefixed with VITE_ to the client.)

# PoC test mode: when 'true', the login page lists the seeded personas as
# one-click sign-in buttons for quick demos. Leave unset/false in production.
VITE_TEST_MODE=true
